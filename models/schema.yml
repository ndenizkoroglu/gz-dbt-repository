version: 2
sources:
  - name: raw
    schema: dbt_dkoroglu      # <-- BigQuery’deki dataset’in buysa böyle kalsın
    tables:
      - name: sales
        identifier: raw_gz_sales
      - name: product
        identifier: raw_gz_product
      - name: ship
        identifier: raw_gz_ship


