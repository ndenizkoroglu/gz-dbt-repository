version: 2

sources:
  - name: raw
    database: deniz-challenge1
    schema: dbt_dkoroglu
    tables:
      - name: product
        identifier: raw_gz_product
        description: "Greenweez ürün kataloğu."
        columns:
          - name: pdt_id          # <= gerçek kolon adı
            description: "Birincil anahtar (ürün kimliği)."
            tests:
              - unique
              - not_null
          - name: purchase_price
            description: "Ürünün satın alma maliyeti."





